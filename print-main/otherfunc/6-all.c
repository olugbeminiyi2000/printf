#include "main.h"
/*Write a function that computes the absolute value of an integer.*/
/**
 * _abs - gives the absolute value of an integer
 *
 * @n: the parameter n of function
 * Return: the absolute value of integer n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (n);
}
